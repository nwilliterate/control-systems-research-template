# Core scientific stack (MATLAB-like numerical computing)
numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pyyaml>=6.0

# Control engineering
control>=0.9.4          # python-control: LQR, transfer functions, state-space

# Rigid-body dynamics (Pinocchio) is intentionally NOT installed here, so this
# plain-pip path stays pure-numpy (matching the README). Get Pinocchio via conda
# (see environment.yml) or the `dynamics` extra:
#   pip install -e '.[dynamics]'   |   uv sync --extra dynamics   (Linux/macOS; Windows -> conda)

# Dev / testing
pytest>=7.0
